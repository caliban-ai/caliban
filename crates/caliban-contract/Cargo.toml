[package]
name        = "caliban-contract"
version.workspace = true
description = "Thin, dependency-light contract crate for caliban: the caliband launch builder (flag/env names) and the supervisor control-plane wire types. No daemon internals — safe for out-of-tree drivers (prospero, caliban-operator) to depend on. Internal crate for caliban; pin exact versions."
edition.workspace      = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true
rust-version.workspace = true

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }

[lints]
workspace = true
